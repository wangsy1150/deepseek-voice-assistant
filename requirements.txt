# 语音交互助手 —— 依赖清单
#
# 两个第三方库：
#   Flask     —— 前端页面渲染、静态文件托管，以及 /api/think、/api/tts 等接口
#   edge-tts  —— 服务端语音合成（调用微软 Edge 的在线语音接口），
#                音质明显好于浏览器原生 TTS，支持传入音色名
#
# 说明：
#   1) 调用 DeepSeek 用的是 Python 标准库 urllib，不需要 requests。
#   2) edge-tts 是「可选但强烈推荐」的：即使安装失败或运行时断网，
#      前端也会自动降级到浏览器原生 speechSynthesis 朗读，功能不受影响。
#      想强制不装，删掉下面一行即可。

Flask>=3.0,<4.0
edge-tts>=7.0,<8.0
